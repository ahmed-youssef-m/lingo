import { Router } from 'express';
// import { createCourse, getCourses, getCourseById, updateCourse, deleteCourse } from '../controllers/courseController';
// import authMiddleware from '../middlewares/authMiddleware';

const router = Router();

// router.post('/createCourse', authMiddleware, createCourse);
// router.get('/getCourses', authMiddleware, getCourses);
// router.get('/getCourse/:id', authMiddleware, getCourseById);
// router.put('/updateCourse/:id', authMiddleware, updateCourse);
// router.delete('/deleteCourse/:id', authMiddleware, deleteCourse);

export default router;